tunnel: grafana-tunnel
credentials-file: /etc/cloudflared/tunnel.json

ingress:
  - hostname: publoader.fuyu.bot
    service: http://localhost:3000  # Make sure this points to your Grafana container
  - service: http_status:404
